{
  "name": "@stryke/reference-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stryke/sdk": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
