{
  "name": "@stryke/reference-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test",
    "start:read-only": "npm run build -w @stryke/sdk && npm run build && node dist/cli.js --read-only",
    "start:live-data": "npm run build -w @stryke/sdk && npm run build && node dist/cli.js --live-data",
    "start:live": "npm run build -w @stryke/sdk && npm run build && node dist/cli.js --live",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@solana/kit": "^7.0.0",
    "@stryke/sdk": "0.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
